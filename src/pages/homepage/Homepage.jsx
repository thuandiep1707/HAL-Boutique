// arr.unshift(arr.pop())
// arr.push(arr.shift())


import './Homepage.scss'

const Homepage = ()=>{
    return(
        <main className="homepage">

        </main>
    )
}

export default Homepage;